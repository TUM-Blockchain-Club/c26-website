# Speaker Application Button Disabled

Date: 2026-05-28

The homepage speaker section no longer shows the `Apply as Speaker` CTA. The old button markup remains commented in `sections/Speaker.tsx` so it can be restored when speaker applications reopen.

The header navigation entry for `Apply as Speaker` was already commented out in `components/header/Header.tsx`.
